[project]
version = "2.0.1"
readme = "README.md"
name = "antimatter-shared-config"
dependencies = [
    "pydantic>=2.0.0",
    "keyring>=24.0.0",
    "cryptography>=41.0.0"
]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
